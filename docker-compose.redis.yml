services:
  redis:
    image: redis:7.2.4
    ports:
      - "6379:6379"
    volumes:
      - ./persistence-data/redis-data:/data
    networks:
      - default

networks:
  default:
    external: true
    name: streamware_default
